-if @user.nil?
  %p No list found.
-else
  #js-react-listContainer
  :javascript
    $(document).ready(function() {
      var listAuthor = '#{@user_name}';
      var currentUser = '#{current_user.name}';
      window.loadListView(listAuthor, #{@list_id}, currentUser);
    });
