-unless @user.nil?
  %h1
    =@user.name
  %p
    Email:
    =@user.email
  %p
    Number of lists:
    =@user.lists.count

  %ul
    -@user.lists.each do |list|
      -if list.public or current_user == @user
        %li
          =link_to list.title, user_list_path(list.user.name, list)
          -if not list.public
            (private)
-else
  No user found with that name.