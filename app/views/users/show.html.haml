
-unless @user.nil?
  .row
    .col.s12
      %h4
        =@user.name
      %p.flow-text
        Email:
        =@user.email
      %p.flow-text
        Number of lists:
        =@user.lists.count

  =render @user.lists
