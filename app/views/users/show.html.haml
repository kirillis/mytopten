-unless @user.nil?
  %h1
    =@user.name
  %p
    Email:
    =@user.email
  %p
    Number of lists:
    =@user.lists.count

  %ul
    -@user.lists.each do |list|
      %li=link_to list.title, user_list_path(list.user.name, list)
-else
  No user found with that name.